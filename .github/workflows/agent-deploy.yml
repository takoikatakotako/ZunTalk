name: Agent Build and Deploy (Cloud Run)

on:
  push:
    branches:
      - main
    paths:
      - 'agent/**'
  workflow_dispatch:
    inputs:
      environment:
        description: Deploy target environment
        required: true
        default: dev
        type: choice
        options:
          - dev
          - prod

env:
  PROJECT_ID: sandbox-492513
  REGION: asia-northeast1

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Resolve deploy target
        id: target
        run: |
          ENVIRONMENT="${{ github.event.inputs.environment || 'dev' }}"
          echo "environment=$ENVIRONMENT" >> "$GITHUB_OUTPUT"
          echo "service=zuntalk-agent-$ENVIRONMENT" >> "$GITHUB_OUTPUT"
          echo "repo=zuntalk-agent-$ENVIRONMENT" >> "$GITHUB_OUTPUT"

      - name: Checkout code
        uses: actions/checkout@v6

      # WIF で GCP に認証。gcp-iac が sandbox に用意した共有 WIF / Terraform SA を借用する。
      # 値は gcp-iac terraform/sandbox の output（wif_provider / terraform_service_account_email）。秘密情報ではない。
      - name: Authenticate to Google Cloud (WIF)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: projects/870183222406/locations/global/workloadIdentityPools/github-actions/providers/github
          service_account: github-actions-terraform@sandbox-492513.iam.gserviceaccount.com

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev --quiet

      - name: Build and push image
        id: build
        working-directory: ./agent
        run: |
          IMAGE="${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ steps.target.outputs.repo }}/${{ steps.target.outputs.service }}:${{ github.sha }}"
          docker build -t "$IMAGE" .
          docker push "$IMAGE"
          echo "image=$IMAGE" >> "$GITHUB_OUTPUT"

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy ${{ steps.target.outputs.service }} \
            --image "${{ steps.build.outputs.image }}" \
            --region ${{ env.REGION }} \
            --project ${{ env.PROJECT_ID }} \
            --quiet
