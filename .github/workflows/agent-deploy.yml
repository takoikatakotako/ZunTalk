name: Agent Build and Deploy (Cloud Run)

on:
  push:
    branches:
      - main
    paths:
      - 'agent/**'
  workflow_dispatch: {}

env:
  PROJECT_ID: sandbox-492513
  REGION: asia-northeast1
  SERVICE: zuntalk-agent
  REPO: zuntalk-agent

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      # WIF で GCP に認証。値は terraform output（wif_provider / deploy_service_account_email）を直書き。
      # 秘密情報ではない。初回 apply 後に値を反映すること（PROJECT_NUMBER は sandbox-492513 の番号）。
      - name: Authenticate to Google Cloud (WIF)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: projects/PROJECT_NUMBER/locations/global/workloadIdentityPools/zuntalk-github-actions/providers/github
          service_account: github-actions-agent@sandbox-492513.iam.gserviceaccount.com

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev --quiet

      - name: Build and push image
        id: build
        working-directory: ./agent
        run: |
          IMAGE="${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPO }}/${{ env.SERVICE }}:${{ github.sha }}"
          docker build -t "$IMAGE" .
          docker push "$IMAGE"
          echo "image=$IMAGE" >> "$GITHUB_OUTPUT"

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy ${{ env.SERVICE }} \
            --image "${{ steps.build.outputs.image }}" \
            --region ${{ env.REGION }} \
            --project ${{ env.PROJECT_ID }} \
            --quiet
